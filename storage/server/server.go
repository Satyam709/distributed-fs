// Package server wires up the gRPC transport for the storage node.
// Service implementations live in storage_handler.go (StorageService) and
// replication_handler.go (ReplicationService).
package server
